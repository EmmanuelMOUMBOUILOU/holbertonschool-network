#!/usr/bin/env bash
# This script changes localhost to resolve to 127.0.0.2 and facebook.com to 8.8.8.8

set -euo pipefail

# Sauvegarde du fichier hosts
sudo cp /etc/hosts /etc/hosts.old

# Supprimer les lignes existantes contenant localhost ou facebook.com
sudo sed -i '/localhost/d' /etc/hosts
sudo sed -i '/facebook\.com/d' /etc/hosts

# Ajouter les nouvelles lignes
echo "127.0.0.2 localhost" | sudo tee -a /etc/hosts > /dev/null
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null
